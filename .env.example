# ===========================================
# Docker Compose Configuration
# ===========================================
# This file contains configuration for Docker Compose services.
# Application-specific settings are in:
#   - Backend: news-backend/.env
#   - Frontend: news-frontend/.env.local
#
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Configure backend: cp news-backend/.env.example news-backend/.env
# 3. Configure frontend: cp news-frontend/.env.local.example news-frontend/.env.local
# 4. Update DB password in both .env and news-backend/.env (must match)
# 5. Update API keys in news-backend/.env
# 6. Run: docker-compose up -d

# ===========================================
# MySQL Service Configuration
# ===========================================
# Note: These must match the values in news-backend/.env
DB_DATABASE=news_db
DB_USERNAME=news_user
DB_PASSWORD=your_secure_password_here

# ===========================================
# Port Mappings (host:container)
# ===========================================
DB_PORT=3307
BACKEND_PORT=8000
FRONTEND_PORT=3000
